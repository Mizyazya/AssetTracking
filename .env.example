# Обов'язково у продакшні: довільний випадковий рядок, підписує cookie сесій.
# Згенерувати: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SECRET_KEY=

# Необов'язково: часовий пояс відображення (за замовчуванням Europe/Kyiv)
# TIMEZONE=Europe/Kyiv
